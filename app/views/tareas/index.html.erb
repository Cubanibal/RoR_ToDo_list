

<h1>Tareas</h1>
  <div class="table-responsive">
    <table class="table table-hover">
      <thead>
        <tr>
          <th scope="col">Descripción</th>
         
          
        </tr>
      </thead>

      <tbody>
        <% @tareas.each do |tarea| %>
          <% if tarea.completada %>
            <tr class="table-secondary">
              <th scope="row" class="tachado"><%= link_to tarea.descripcion, tarea %></th>
             
            </tr>
          <% else %>
          <tr>
              <th scope="row"><%= link_to tarea.descripcion, tarea %></th>
              
            </tr>
          <% end %>  
        <% end %>
      </tbody>
    </table>
  </div>
<br>

<%= link_to 'Nueva Tarea', new_tarea_path, class: "btn btn-outline-primary" %>
